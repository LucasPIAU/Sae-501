export const selectToken = (state) => state.connexion.token;
export const selectLoading = (state) => state.connexion.loading;
export const selectError = (state) => state.connexion.error;
export const selectIsConnected = (state) => state.connexion.isConnected;